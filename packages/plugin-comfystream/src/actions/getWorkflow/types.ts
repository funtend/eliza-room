export interface Workflow {
    workflowMetadata: {
        name: string;
        description: string;
        tags: string;
        workflow: string;
        cover: string;
    };
    creator: string;
}
